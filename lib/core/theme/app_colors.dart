import 'package:flutter/material.dart';

/// Application color palette
class AppColors {
  AppColors._();

  // Primary colors
  static const Color primary = Color(0xFF2196F3);
  static const Color primaryDark = Color(0xFF1976D2);
  static const Color primaryLight = Color(0xFF64B5F6);

  // Secondary colors
  static const Color secondary = Color(0xFF4CAF50);
  static const Color secondaryDark = Color(0xFF388E3C);
  static const Color secondaryLight = Color(0xFF81C784);

  // Background colors
  static const Color background = Color(0xFFF5F5F5);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceVariant = Color(0xFFF5F5F5);

  // Text colors
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textDisabled = Color(0xFFBDBDBD);

  // Status colors
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFF44336);
  static const Color warning = Color(0xFFFF9800);
  static const Color info = Color(0xFF2196F3);

  // Priority colors
  static const Color priorityLow = Color(0xFF4CAF50);
  static const Color priorityMedium = Color(0xFFFF9800);
  static const Color priorityHigh = Color(0xFFF44336);

  // Chart colors
  static const Color chartTemperature = Color(0xFFFF5722);
  static const Color chartHumidity = Color(0xFF2196F3);
  static const Color chartLight = Color(0xFFFFEB3B);
  static const Color chartNoise = Color(0xFF9C27B0);
  static const Color chartTvoc = Color(0xFF4CAF50);

  // Recommendation type colors
  static const Color recommendationVentilate = Color(0xFF03A9F4);
  static const Color recommendationLighting = Color(0xFFFFEB3B);
  static const Color recommendationNoise = Color(0xFF9C27B0);
  static const Color recommendationBreak = Color(0xFF4CAF50);
  static const Color recommendationTemperature = Color(0xFFFF5722);
  static const Color recommendationHumidity = Color(0xFF2196F3);

  // Device status colors
  static const Color deviceActive = Color(0xFF4CAF50);
  static const Color deviceInactive = Color(0xFF757575);

  // Border colors
  static const Color border = Color(0xFFE0E0E0);
  static const Color borderDark = Color(0xFFBDBDBD);

  // Divider color
  static const Color divider = Color(0xFFE0E0E0);

  // Overlay colors
  static const Color overlay = Color(0x80000000);
  static const Color overlayLight = Color(0x40000000);
}
